PG:::

CREATE TABLE t (
	id SERIAL NOT NULL, 
	is_default BOOLEAN DEFAULT '0' NOT NULL, 
	PRIMARY KEY (id)
)


SQLITE:::

CREATE TABLE t (
	id INTEGER NOT NULL, 
	is_default BOOLEAN DEFAULT '0' NOT NULL, 
	PRIMARY KEY (id)
)


